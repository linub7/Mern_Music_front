const AuthFormTitle = ({ title }) => {
  return (
    <h1 className="text-transparent font-bold text-3xl bg-clip-text bg-gradient-to-r from-purple-400 to-pink-600">
      {title}
    </h1>
  );
};

export default AuthFormTitle;
